// You can add simple Javascript here if needed
// For example:

const messageElement = document.getElementById('message');

if (messageElement) {
   messageElement.textContent = "This text is modified by javascript";
}